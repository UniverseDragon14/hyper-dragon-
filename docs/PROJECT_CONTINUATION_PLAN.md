# Universal Dragon Project Continuation Plan

Date: 2026-07-08
Owner: Aslam
Official public identity: Universal DRAGON Aslam
Project family: Universal Dragon / NOVA / EVE / UDOS / QBIT NOVA

## Branding Lock

Use this public identity consistently:

```text
Universal DRAGON Aslam
```

Meaning:

```text
Universal DRAGON = main project identity
Aslam = creator / owner / architect
NOVA = stable brain direction
EVE = assistant and app-builder layer
UDOS = assistant-first operating/control layer concept
QBIT NOVA = language / core engine direction
```

Do not split this into random new project names unless there is a clear reason.

## Decision

Do not open a new project now.
Continue from the existing GitHub project with the strongest foundation.

Main continuation repository:

```text
UniverseDragon14/Universal-Dragon-Core
```

Reason: this repository already represents the Universal Dragon brain and QBIT NOVA language direction. It has the clearest identity, version branch, examples, and test workflow.

## Repository Ranking

| Rank | Repository | Role | Decision |
|---:|---|---|---|
| 1 | `Universal-Dragon-Core` | Main NOVA / QBIT NOVA brain and language core | Continue first |
| 2 | `qbit-nova-c` | C engine, compiler, VM, QMSG, OpenQASM proof layer | Continue as technical engine support |
| 3 | `UniverseDragon14.github.io` | Public portfolio, proof pages, SEO, project showcase | Keep synced after core progress |
| 4 | `studio-site` | Creator studio / web app / video tool direction | Continue after core roadmap is stable |
| 5 | `eve-app-builder` | EVE app builder workflow and approval-first rules | Use as builder process reference |
| 6 | `nova-agi-core-backend` | Backend API direction for NOVA Guardian | Clean wording and make professional before expanding |
| 7 | `udos-site` | Assistant-first control layer concept | Long-term direction, not first priority |
| 8 | `askutty-cloud-brain` | Notes, continuity, Pi5/mobile planning support | Support repo only |

## Main Rule

```text
No new project until the existing main repo has a clear roadmap, working checks, and public proof sync.
```

## Current Focus

Universal Dragon should move in this order:

1. Stabilize `Universal-Dragon-Core`.
2. Keep `qbit-nova-c` as the technical proof engine.
3. Sync public progress to `UniverseDragon14.github.io`.
4. Use `studio-site` later for practical creator tools.
5. Use `eve-app-builder` as the approval-first app creation rulebook.

## Immediate Tasks

### Phase 1: Core cleanup

- Confirm default branch and active branch.
- Check `nova doctor` command direction.
- Check example files under `examples/`.
- Confirm QBIT NOVA `.ud` identity.
- Add or update changelog.
- Add a small issue/task list.

### Phase 2: Proof sync

- Link QBIT NOVA proof from the public website.
- Keep `UniverseDragon14.github.io` as the public proof layer.
- Avoid mixing website files into core engine files.

### Phase 3: Engine support

- Keep `qbit-nova-c` as the C-based proof engine.
- Do not overclaim physical quantum hardware.
- Keep it clearly described as a software virtual quantum processor layer.

### Phase 4: Studio and app builder later

- Use `studio-site` for practical creator tools.
- Use `eve-app-builder` for the discuss -> plan -> approval -> build -> test workflow.
- Do not build random new apps without checking existing repos first.

## Public Safety Rules

- No private keys.
- No exposed tokens.
- No private runtime secrets.
- No destructive automation.
- No hidden actions.
- Backup before patch.
- Rollback always ready.
- Approval before risky actions.

## Summary

The Universal Dragon continuation path is:

```text
Universal-Dragon-Core -> qbit-nova-c -> UniverseDragon14.github.io -> studio-site -> eve-app-builder
```

This keeps the project focused instead of creating another empty repo.
